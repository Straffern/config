return {
	"mason-org/mason.nvim",
	opts = {
		registries = {
			"github:mason-org/mason-registry", -- Default registry
			"github:mimikun/mason-registry", -- Your extra registry (replace <user>/<repo>)
		},
	},
}
